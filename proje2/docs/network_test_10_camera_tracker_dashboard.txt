Proje2 Network Test 10: Camera Tracker Dashboard
================================================

Status
------

```text
LIVE PASS - CSI camera tracker dashboard
```

Goal
----

Prove that camera-derived tracker telemetry reaches the same dashboard and still
travels through the proje1 CRC/QPSK/OFDM bridge:

```text
Orin camera tracker packet
  -> UDP port 5013
  -> proje1 tracker_udp_comm_bridge.py
  -> UDP port 5011
  -> proje2 dashboard
```

PC Commands
-----------

Terminal 1:

```powershell
cd "C:\Users\Msi\Documents\yaz proje"
python proje2/code/pc_app/solar_live_dashboard.py --udp-host 0.0.0.0 --udp-port 5011 --http-host 127.0.0.1 --http-port 8085 --bess-load-w 0.1 --log proje2/data/solar_live_dashboard_camera_tracker_log.csv
```

Terminal 2:

```powershell
cd "C:\Users\Msi\Documents\yaz proje"
python proje1/code/pc_app/tracker_udp_comm_bridge.py --bind-host 0.0.0.0 --bind-port 5013 --forward-host 127.0.0.1 --forward-port 5011 --log proje1/data/tracker_udp_comm_bridge_camera_tracker_log.csv
```

Open:

```text
http://127.0.0.1:8085
```

Orin Synthetic First
--------------------

```bash
cd /home/msi
python3 proje3/code/orin_app/camera_light_tracker_udp_gateway.py --synthetic --udp-host 192.168.1.219 --udp-port 5013 --max-packets 80 --interval 0.25
```

Orin CSI Camera Second
----------------------

```bash
cd /home/msi
python3 proje3/code/orin_app/camera_light_tracker_udp_gateway.py --camera csi --camera-id 0 --width 640 --height 480 --udp-host 192.168.1.219 --udp-port 5013 --max-packets 80 --interval 0.25
```

Expected Dashboard
------------------

```text
Source:
  Orin CSI camera solar tracker

MPPT Status:
  CV Tracking

Vision row:
  Vision Mode = synthetic/csi
  Vision Detection = yes
  Vision Confidence > 0
  Target Pixel changes
  Target Offset changes

Network row:
  Link State = healthy
  Invalid Packets = 0
  Sequence Gaps = 0

Digital Comm Layer:
  CRC/QPSK/OFDM
```

Pass Criteria
-------------

```text
PASS when camera tracker packets appear on the dashboard with vision fields and
the proje1 bridge metadata remains CRC/QPSK/OFDM with zero bad packets and zero
sequence gaps during a clean run.
```

Live Evidence - 2026-08-07
--------------------------

Observed live path:

```text
Orin IMX219 CSI camera tracker
  -> UDP port 5013
  -> proje1 tracker_udp_comm_bridge.py
  -> UDP port 5011
  -> proje2 dashboard
```

Observed bridge output:

```text
OK from=192.168.1.30 p1br,frame_b=00133,bits=01064,qpsk=00532,ofdm=012,pad_q=00044,gap=0,ok=1
```

Observed dashboard output:

```text
Source = Orin CSI camera solar tracker
Power = 1.60 W
Voltage = 5.30 V
Current = 0.30 A
Link State = healthy during active run
Invalid Packets = 0
Sequence Gaps = 0
Digital Comm Layer = CRC/QPSK/OFDM
Tracker State = locked
Panel Angles example = 70.0 / 100.0
Vision Mode = csi
Vision Detection = yes
Vision Confidence = 98%
Target Pixel example = 20 / 202
Target Offset example = X -0.939, Y 0.158
Frame / Bright Area = 640x480 / 6300 px
Actuator = servo
```

Note:

```text
The dashboard showed stale after the finite Orin --max-packets run stopped.
This is expected when no new packets arrive after the test window.
```
