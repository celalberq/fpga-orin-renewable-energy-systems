HDL Folder
==========

Optional future network-HDL scope:

```text
eth_header_parser
ipv4_parser
tcp_udp_parser
flow_counter
metadata_fifo
telemetry_uart
top_network_monitor
```

These modules are not part of the validated final prototype. Current Proje2
proof is the PC-side UDP transport, validation, reliability proxy, and
dashboard path. The only present XDC is a baseline Nexys Video LED constraint.
