First Tests Without Orin: fpga-digital-communications Digital Communication
======================================================

Temporary host:

```text
Laptop/PC replaces Orin for now.
```

First tests:

```text
1. Python shared packet framing + CRC simulation: DONE
2. FPGA shared packet frame/CRC loopback over UART: DONE
3. Python QPSK packet-link simulation: DONE
4. Python BER vs noise CSV for QPSK packet link: DONE
5. FPGA QPSK mapper/demapper loopback over UART: DONE
6. HDL QPSK mapper testbench.
7. HDL demapper testbench.
8. FPGA channel/noise loopback counter: DONE
9. OFDM framing plan and first simulation: DONE
10. FPGA OFDM resource-grid counter: DONE
11. FPGA OFDM cyclic-prefix/resource-grid sequencer: READY FOR BOARD
12. UART telemetry to PC:
   p1,seq=00001,payload=p3,len=00083,inj=0,tx=0C,rx=0C,crc_fail=00000,undet=00000,mask=00,ok=1
```

Nexys Video features to use first:

```text
1. LEDs for link state.
2. Switches for mode selection.
3. Buttons for reset/start.
4. USB-UART for telemetry.
```

Do not start with:

```text
1. RF hardware.
2. SDR.
3. External ADC/DAC.
```

Definition of ready for next stage:

```text
Shared packet framing/CRC simulation works, then basic FPGA frame/CRC telemetry is
visible on the PC, then QPSK loopback works in simulation and on the FPGA.
```
