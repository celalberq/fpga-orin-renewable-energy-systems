Pin Maps Folder
===============

No separate communication pin-map artifact is required for the final build.
The exact Nexys Video clock, UART, LED, and switch assignments are authoritative
in the XDC files under `../constraints` and their matching board-test records.
